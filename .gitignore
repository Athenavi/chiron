# ── Go ──
*.exe
*.exe~
*.dll
*.dylib
*.test
*.out
go.work
build/

# ── Python ──
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# ── Node / Vue ──
node_modules/
.next/
out/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── Environments ──
.env
# .env 的备份/变体同样含密钥。APP_SECRET 是唯一根密钥（派生 JWT_SECRET /
# INTERNAL_TOKEN 与配置加密密钥），一个 `git add -A` 提交进去 = 全盘失守。
.env.*
!.env.example
*.bak
.envrc
.venv
env/
venv/
ENV/

# ── Runtime data ──
workspace/
backend/
*.db
*.sqlite*
*.rdb
*.pid
.pids/
logs/
*.jsonl

# ── Reasonix / local tooling ──
.reasonix/
.agents/
reasonix.toml
.cowork-temp/
**/.cowork-temp/

# ── Test data ──
test_register.json
*.log
stderr.log
stdout.log

# ── Misc ──
third_party/
sandbox/
/plugins.json
/frontend-vue/public/vendor/

# ── 第三方参考源码 ──
# 这里原先逐文件列出了 /third/DeepSeek-Reasonix-main-v2/ 的 6604 个路径
# （该目录已不在工作区），使本文件膨胀到 6677 行 / 504KB。
# 改为目录级忽略：语义等价，新增文件无需再逐条登记。
/third/

# 数据库凭据（本地明文，仅用于交接记录，禁止入库）
.pg-credentials.txt
